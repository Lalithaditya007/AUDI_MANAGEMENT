{
  "version": 2,
  "installCommand": "npm install --prefix server",
  "buildCommand": "echo Using prebuilt client/dist",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
